fastapi>=0.115.0
uvicorn[standard]>=0.30.6
SQLAlchemy>=2.0.35
pydantic>=2.9.2
pydantic-settings>=2.5.2
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# Pinned: passlib 1.7.4's bcrypt backend version-probe is incompatible
# with bcrypt>=4.1 (ValueError: password cannot be longer than 72 bytes)
# and breaks every hash/verify call, including login and demo-user
# seeding at startup. Keep pinned until passlib ships a fix, or replace
# passlib's bcrypt handler entirely.
bcrypt>=4.0.1,<4.1
python-multipart>=0.0.9
httpx>=0.27.2
websockets>=13.0
python-dotenv>=1.0.1
psycopg2-binary>=2.9.9
redis>=5.0.8
email-validator
